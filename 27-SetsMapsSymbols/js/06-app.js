//ESTO ES DE ITERADORES 

function crearIterador(carrito) {
    
    let i = 0;

    return{
        siguiente: ()=>{

        }
    }
}
const carrito = ['Producto1', 'Producto2','Producto3'];

//UTILIZAR EL ITERADOR 
const recorrerCarrito = crearIterador(carrito);

console.log(recorrerCarrito.siguiente());